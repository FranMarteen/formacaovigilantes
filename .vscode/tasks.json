{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Dev Server",
      "type": "shell",
      "command": "npm run dev",
      "options": { "cwd": "${workspaceFolder}/vercel-setup" },
      "problemMatcher": []
    },
    {
      "label": "Build Site (Static Export)",
      "type": "shell",
      "command": "npm run build",
      "options": { "cwd": "${workspaceFolder}/vercel-setup" },
      "problemMatcher": []
    },
    {
      "label": "Export + RSS + Sitemap",
      "type": "shell",
      "command": "npm run build && npm run export",
      "options": { "cwd": "${workspaceFolder}/vercel-setup" },
      "problemMatcher": []
    }
  ]
}
